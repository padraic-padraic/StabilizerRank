#include <stdlib.h>
#include <math.h>
#include <complex.h>
#include <binaryform.h>
#include <expsum.h>
#include <innerproduct.h>

complex InnerProduct(stabiliser *phi1, stabiliser *ph1)
{
    affine_sp a = *(phi1->a); //This won't work. I need to define a copy method
    quadratic_fm q;
    affine_sp *a1 = phi1->a, *a2 = phi2->a;
    quadratic_fm *q1 = phi1->q, *q2 = phi2->q;
    for (int i = phi)
    return pow(2, -1*(phi1->a->k-phi2->a->k)/2)*ExponentialSum(q);
}